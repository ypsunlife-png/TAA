# TAA

This repository currently contains the project under `lias-sug/`.

## Project location
- `lias-sug/`

## Quick start
```bash
cd lias-sug
npm install
npm test
npm run generate-openapi
```

## Notes
- Main source and test files are inside `lias-sug/`.
- You can move files to repository root later if needed.
